# Ignore files and folders that should not be baked into the image

# VCS & CI
.git
.github

# Local environment
.env
.env.*

# Node
node_modules
public/hot

# Composer vendor from host (built in image instead)
vendor

# Laravel caches, logs, and storage content
bootstrap/cache
storage/*

# Keep an empty directory structure but not host files
!storage/.gitignore
!bootstrap/cache/.gitignore

# Tests and tooling
tests
docker-compose.yml
docker-compose.*.yml
Dockerfile*

